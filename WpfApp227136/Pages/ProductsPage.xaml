<Page x:Class="WpfApp227136.Pages.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp227136.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductsPage" IsVisibleChanged="Page_IsVisibleChanged"> 
    

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel x:Name="ProductStackPanel" Orientation="Horizontal">
            <TextBox x:Name="SearchTextBox" 
                     Width="400" 
                     TextChanged="SearchTextBox_TextChanged"/>
            <ComboBox x:Name="PriceSortComboBox" 
                      SelectedIndex="0"  
                      SelectionChanged="PriceSortComboBox_SelectionChanged">
                <ComboBoxItem>Сортировка</ComboBoxItem>
                <ComboBoxItem>По возрастанию</ComboBoxItem>
                <ComboBoxItem>По убыванию</ComboBoxItem>
            </ComboBox>
            <ComboBox x:Name="ProductTypeComboBox" Width="200" SelectedIndex="0"
                      DisplayMemberPath="type_product" 
                      SelectionChanged="ProductTypeComboBox_SelectionChanged"/>
        </StackPanel>
        <TextBlock x:Name="FIOTextBlock" Grid.Row="0" HorizontalAlignment="Right"/>
        <ListBox x:Name="ProductsListBox" 
                 Grid.Row="1" 
                 HorizontalContentAlignment="Stretch"
                 MouseDoubleClick="ProductsListBox_MouseClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="80"/>
                        </Grid.ColumnDefinitions>
                        <Image Grid.Column="0" Width="70" Height="50">
                            <Image.Source>
                                <Binding Path="picture">
                                    <Binding.TargetNullValue>
                                        <ImageSource>../Resourses/Восьмерка.png</ImageSource>
                                    </Binding.TargetNullValue>
                                </Binding>
                            </Image.Source>
                        </Image>
                        <StackPanel Grid.Column="1">
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{} {0} | {1}">
                                        <Binding Path="type_products.type_product"/>
                                        <Binding Path="name_product"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                            <TextBlock Text="{Binding art}"/>
                            <TextBlock Text="{Binding Materials, StringFormat=Материалы: {0}}"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="{Binding min_price}"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsGreatThen10K}" Value="True">
                            <Setter Property="Background" Value="#BB5FCA"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center">
            <Button x:Name="MaterialButton"
                Content="Материалы"
                Click="MaterialButton_Click"/>
            <Button x:Name="AddButton" Content="Добавить продукцию" Click="AddButton_Click"/>
            <Button x:Name="DelButton" Content="Удалить" Click="DelButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
